class Solution {
  String longestCommonPrefix(List<String> strs) {
    bool doNotBreak = true;

    while (doNotBreak) {
      for (int j = 0; j < strs.length; j++) {
        String currentString = strs[j];

        String currentChar = currentString[j];
      }
    }
  }
}
